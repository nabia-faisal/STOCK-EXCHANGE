/**
 * Domain model: core entities of the exchange.
 *
 * <p>Contains the OOP hierarchies that demonstrate inheritance, polymorphism,
 * and encapsulation: {@code Order} (abstract) with {@code MarketOrder} and
 * {@code LimitOrder}; {@code Trader} (abstract) with {@code RetailTrader},
 * {@code InstitutionalTrader}, and {@code Admin}; plus {@code Stock},
 * {@code Portfolio}, {@code Holding}, and {@code Trade}.</p>
 */
package com.nust.exchange.model;
