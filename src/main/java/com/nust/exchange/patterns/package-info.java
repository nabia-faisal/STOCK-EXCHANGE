/**
 * Reusable design-pattern support types.
 *
 * <p>{@code Observer}/{@code Observable} for propagating market updates to the
 * UI and bots, and {@code OrderFactory} for constructing order subtypes.</p>
 */
package com.nust.exchange.patterns;
